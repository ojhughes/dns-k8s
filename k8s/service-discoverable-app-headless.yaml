apiVersion: v1
kind: Service
metadata:
  labels:
    app: discoverable-app-headless
  name: discoverable-app-headless
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: discoverable-app-headless
  type: ClusterIP
status:
  loadBalancer: {}
